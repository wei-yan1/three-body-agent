# Conversation Archives

This directory is reserved for exported conversation archives.

Runtime storage should primarily live in MySQL. Files here can be used for
manual exports, fixtures, or offline review.

Suggested archive metadata:

- `session_id`
- `character`
- `timeline_stage`
- `created_at`
- `message_count`
- `persona_skill_id`
- `consistency_check_status`
